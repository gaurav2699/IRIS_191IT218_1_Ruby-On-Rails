<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @book.description %>
</p>

<p>
  <strong>Quantity:</strong>
  <%= @book.quantity %>
</p>

<p>
  <strong>Isbn:</strong>
  <%= @book.isbn %>
</p>
<p>
  <% (0...@book.images.count).each do |image| %>

      <%= image_tag(@book.thumbnail(image))%>

  <% end %>
</p>
<% if current_user.id==$users.first.id %>
  <%= link_to 'Edit', edit_book_path(@book) %> |
<%end%>
<%= link_to 'Back', books_path %>
